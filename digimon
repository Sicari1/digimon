#!/usr/bin/env bash
# 상태창 실행 래퍼. 심볼릭 링크로 호출돼도 실제 위치를 찾는다.
# 상태 박스(현재 쓰는 claude-usage.py --box)를 띄운다.
DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")" && pwd)"
exec /usr/bin/python3 "$DIR/claude-usage.py" --box "$@"
